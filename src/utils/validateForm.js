export const validateForm = (data, documents, agreed) => { const errors = {}; ["fullName","dob","ageGroup","gender","category","club","coachName","state"].forEach(key => { if (!data[key]) errors[key] = "This field is required."; }); if (!/^[6-9]\d{9}$/.test(data.mobile || "")) errors.mobile = "Enter a valid 10-digit Indian mobile number."; if (!data.races?.length) errors.races = "Select at least one race."; if (!documents.aadhaarCard) errors.aadhaarCard = "Aadhaar card is required."; if (!documents.dobCertificate) errors.dobCertificate = "DOB certificate is required."; if (!agreed) errors.agreed = "Please accept the rules."; return errors; };
