export default function PaymentSummary({ handleRegistration, agreed, setAgreed, errors, setErrors, loading }) {
  return <section className="registration-card"><div className="section-kicker">SECTION 4</div><h2>Payment &amp; Confirmation</h2><p className="upload-note">Your registration is saved only after secure payment verification.</p><div className="payment-row"><span>Registration Fee</span><strong>₹500</strong></div><hr /><div className="payment-row total"><span>Total Amount</span><strong>₹500</strong></div><label className={`injury-declaration ${errors.agreed ? "input-error" : ""}`}><input type="checkbox" checked={agreed} onChange={(e) => { setAgreed(e.target.checked); if (e.target.checked) setErrors((old) => ({ ...old, agreed: "" })); }} /><span><strong>Parent/Guardian Declaration *</strong>I confirm that if any injury occurs during the Championship, the participant’s parent or guardian will be responsible.</span></label>{errors.agreed && <small className="error-text">{errors.agreed}</small>}<button className="payment-btn" onClick={handleRegistration} disabled={loading}>{loading ? "Please Wait..." : "Proceed to Payment"}</button></section>;
}
